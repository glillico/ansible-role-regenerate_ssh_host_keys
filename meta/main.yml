galaxy_info:
  role_name: regenerate_ssh_host_keys
  author: glillico
  description: Creates a service that regenerates the sshd host keys on server boot.
  license: license (MIT)
  min_ansible_version: 2.4
  platforms:
    - name: Debian
      versions:
        - stretch
        - buster
        - bullseye
    - name: Ubuntu
      versions:
        - bionic
        - focal
    - name: EL
      versions:
        - 7
        - 8
  galaxy_tags:
    - ssh
    - sshd
dependencies: []
